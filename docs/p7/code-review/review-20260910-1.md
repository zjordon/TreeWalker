Review complete: 6 finding(s) across 5 selected item(s).

─── .gitignore:239-240 ───
[maintainability · low] `out/` is unanchored, so it ignores every directory named `out` at any depth
of the repo. The intended target is always repo-root `out/` (examples/p7_* default `--out` resolves
to `REPO_ROOT / "out" / ...`), and the `/registry/` entry directly below documents the convention of
anchoring such rules to the repo root to avoid shadowing same-named nested directories. Suggest
anchoring as `/out/` for precision and consistency with that convention.

- # 本地脚本产物（examples/p7_* 默认 --out 落点，如任务级 skill 匹配离线回归）
- out/
+ # 本地脚本产物（examples/p7_* 默认 --out 落点，如任务级 skill 匹配离线回归）；锚定仓库根
+ /out/


─── src/tree_walker/skills/task_matcher.py:231-233 ───
[bug · medium] The normalized task_kind (and match_kind) computed here are dropped by all three
post-parse early returns below — the null-slug, unknown-slug, and low-confidence downgrade paths
each construct TaskSkillMatch without task_kind, so it silently falls back to None on every no-hit
path. This contradicts the documented S4 log contract in agent.py ("task_kind 是用户任务属性，无论命中与否照记"),
which logs match.task_kind unconditionally — misses will always log null — and skews the regression
harness's task_kind distribution metric (test_task_skill.py::test_match_log_carries_tier_fields
mocks match_task_skill with TaskSkillMatch(slug=None, task_kind="read"), a state the real matcher
can never produce, so this drop is untested). Pass task_kind=task_kind in the three early-return
constructors (match_kind is moot when slug is None since the agent logs it as null on miss).

-     task_kind = str(result.get("task_kind") or "").strip().lower()
-     if task_kind not in ("read", "operate"):
-         task_kind = None
+     if slug.lower() in _NULL_SLUG_LITERALS:
+         return TaskSkillMatch(
+             slug=None, confidence=confidence, reason=reason, task_kind=task_kind
+         )
+     if slug not in known_slugs:
+         ...
+         return TaskSkillMatch(
+             slug=None, confidence=confidence, reason=f"unknown slug: {slug}", task_kind=task_kind
+         )
+     if confidence not in ("high", "medium"):
+         ...
+         return TaskSkillMatch(
+             slug=None, confidence=confidence, reason=reason, downgraded=True, task_kind=task_kind
+         )


─── examples/p7_task_skill_match_regression.py:226-228 ───
[bug · low] vp['correct'] / vp['total'] divides unconditionally. With --limit small enough that the
selected task prefix contains no variant tasks (variant_rows empty), this line raises
ZeroDivisionError before args.out.write_text runs, so a --limit smoke run loses its results
entirely. The --gate check below (vp["correct"] / vp["total"] >= GATE_VARIANT_RATE) has the same
issue. Guard the ratio when vp['total'] == 0.

+ 	variant_rate = vp["correct"] / vp["total"] if vp["total"] else 0.0
  	print(f"泛化集:  命中 {vp['hit']}/{vp['total']}"
  	      f" | 正确模板 {vp['correct']}/{vp['total']}"
- 	      f"（{vp['correct'] / vp['total']:.1%}）"
+ 	      f"（{variant_rate:.1%}）"


─── examples/p7_task_skill_match_regression.py:158-158 ───
[bug · low] card_template is keyed only by replay-map slugs, but m.slug can be any catalog slug
(match_task_skill validates against the catalog's known_slugs, not the replay map). Startup
validation only checks the reverse direction (replay task_ids ⊆ task set); if domain-skills gains a
card without a replay mapping, this raises KeyError during metrics computation — after all 184×N LLM
calls have already completed, losing the whole run's results. Fail fast at startup on catalog slugs
missing from the replay map.

- 			"correct_template": m.slug is not None and card_template[m.slug] == tpl,
+ 	replay = load_replay(args.eval_root, tasks_by_id)
+ 	unmapped = {c.slug for c in catalog} - set(replay)
+ 	if unmapped:
+ 		raise SystemExit(f"catalog 卡缺 replay 映射: {sorted(unmapped)}——card_template 会 KeyError")


─── examples/p7_task_skill_match_regression.py:87-88 ───
[maintainability · low] Failure detection here relies on the reason string literally starting with
"call failed", an implicit contract with task_matcher.py's `reason=f"call failed: {e}"` (currently
matching, verified). If that reason format is ever reworded, retries silently stop and every
transient API failure is counted as a miss — directly biasing the variant-rate gate metric. Make the
failure mode explicit instead of prefix-matching free-form text, e.g. a `call_failed: bool` field on
TaskSkillMatch (set on the exception/timeout path), or at minimum a shared constant both modules
import.

  			m = await match_task_skill(task_text, catalog, llm)
- 			if not m.reason.startswith("call failed"):
+ 			if not m.call_failed:  # TaskSkillMatch.call_failed: bool — 由 matcher 异常/超时路径置位
+ 				return m


─── src/tree_walker/skills/task_matcher.py:86-92 ───
[bug · medium] `match_kind` and `task_kind` are missing the `enum` constraints that the design docs
explicitly specify (docs/p7/04 §4.2: `"enum": ["same_task", "same_template", null]` / `["read",
"operate", null]`; docs/p7/03 §附录 likewise). `structured_call` forwards `output_schema` verbatim as
the tool's `input_schema` (client.py:574–588), so on the tool-forced path the enum would steer the
model to valid values — the same reason `confidence` keeps its enum here. Without it, the model can
emit off-enum strings (e.g. "variant", "query") which the parse-side whitelist then silently
collapses to `same_task`/`None`: a genuine same-template match loses the entity-substitution warning
header and a read task loses the anti-parroting appendix, i.e. the tiering silently degrades back to
the v2 single tier. The whitelist was designed as a guard for the text-fallback path (which does no
schema validation), not a substitute for schema enforcement on the tool path. Suggest adding the
enums per the plan (JSON `null` in the enum list keeps the field optional-consistent with `required`
staying `["match", "confidence", "reason"]`).

          "match_kind": {
              "type": ["string", "null"],
+             "enum": ["same_task", "same_template", None],
              "description": (
                  "same_task = same template AND same entity values; "
                  "same_template = same template with different entity values; null when no match."
              ),
+         },
+         "task_kind": {
+             "type": ["string", "null"],
+             "enum": ["read", "operate", None],
+             "description": "Whether the user task reads a fact from the site (read) or changes site state (operate).",
          },

