# Code Review（第一轮）：PR #177（feat/175-llm-vision）

- **日期**：2026-09-07
- **目标**：`feat/175-llm-vision` 分支（LLM 视觉通道接入 GLM-5.3-Flash：`use_vision` 门控 + image block 回流 + 截图入历史）
- **范围**：全量 diff 约 2076 行（`master...HEAD`），tests/ hunks 按 low-effort 规则跳过
- **方法**：低档（单遍 diff、不验证、仅限 hunk 内的运行时正确性检查）
- **验证结论**：**无发现**（0 条 finding）

## 覆盖范围

非测试文件：

- `src/tree_walker/agent/agent.py` — use_vision / llm_screenshot_size 配置拷贝
- `src/tree_walker/agent/message_compactor.py` — `_content_text` block list 兼容
- `src/tree_walker/agent/step.py` — 视觉门控、`_prepare_state_screenshot_b64`、`_set_state_message` 旧 state 滤图、`_save_conversation` dump 兼容、`_finalize` 截图落盘
- `src/tree_walker/agent/views.py` — `screenshot_path` 字段注释更新
- `src/tree_walker/browser/session.py` — `captureScreenshot` 包 `asyncio.wait_for` 超时降级
- `src/tree_walker/config.py` — `model_supports_vision` 名单、`_parse_screenshot_size`、`screenshot_timeout`
- `src/tree_walker/llm/client.py` — 两过滤器 block list 适配、fallback 滤图 `_strip_image_blocks`
- `src/tree_walker/prompts/system_prompt.py` — `build_state_blocks`
- `examples/`（e2e_vision_channel / smoke_vision_glm53 / upload_file_vision / vision_mode）、`ROADMAP.md`、`docs/tools-optimize/screenshot.md`、`uv.lock`

## 检查过的可疑点（均排除）

1. **step.py `build_state_blocks(browser_state, screenshot_b64, **state_kwargs)` 疑似重复传参** — 核对 hunk：`browser_state=browser_state` 行已被删除，`state_kwargs` 不含该键，无 `TypeError`。
2. **`_set_state_message` 旧 state 滤图** — 先 drop 再 strip 再 append，新消息不受影响，顺序正确。
3. **`_strip_image_blocks` 原地修改共享消息 dict** — 与 fallback 后模型永久切换的语义一致（门控每步按 `llm.model` 重估），非 bug。
4. **`model_supports_vision` 正则** `^glm-\d+(\.\d+)*v` 与 `glm-5.3-flash` 显式前缀，无交叉误判。
5. **session.py 超时** `if timeout and timeout > 0`（0=关闭防护）与 docstring 一致，`wait_for` 超时走外层 `except Exception` → warning + raise → get_state 降级 None，链路闭合。
6. **`_finalize` 存原图** — resize 在 `_prepare_state_screenshot_b64` 内做且不回写 `browser_state.screenshot`，落盘确为原图。

## 结论

Hunk 内未发现反转条件、越界、空解引用、缺失 await、复制粘贴错变量、吞错等运行时正确性问题；亦无 hunk 内可见的重复 helper 或死代码。最终结论：**无发现**（0 条 finding，未达 4 条上限）。

低档只报高置信度问题；如需更深入审查（跨 hunk 交互、状态机语义、并发时序），可再跑 `/code-review 177 high`。
