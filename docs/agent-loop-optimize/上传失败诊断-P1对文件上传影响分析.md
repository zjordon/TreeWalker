# 诊断：P1 改动是否影响了文件上传（抖音封面上传失败）

> **结论：不影响。** P1（context-enrichment）改动完全没触碰上传路径；封面失败是
> 抖音 Semi UI 隐藏 file input 不响应 `DOM.setFileInputFiles` 的**既有问题**。
> 本次为**分析报告，不做任何代码改动**。
>
> ⚠️ **二次修订（2026-07-05）：根因再次更正——是 P0 的 state 替换，不是 P1c。**
> A/B 实测（P0 前 3/3 成、P0 失败、`enable_message_typing=false` workaround 修复）证明
> 根因是 P0 把每步 state 从「累积」改成「全局唯一替换」，让模型丧失前后 DOM 对比能力；
> 已落地 Option A 修复（`_set_state_message` 保留最近 2 份 state）。
> **请以文末「二次修订」小节为准**——一次「修订」里的 P1c 结论已被本次覆盖（P1c 的
> 警告放大机制代码层面成立、其 `_summarize_step_result` 修复也保留，但不是这次回归的根因）。

## 1. 失败现象（来自 `D:\temp\donying-upload.log`）

- **Step 1**：视频 `2026-04-29-20-41-59.mp4` 上传 `DOM.setFileInputFiles` → **成功**，页面进入下一步。
- **Step 5–18**：封面 `heng.png` 反复 `upload_file` 到 `semi-upload-hidden-input`（索引 32197→32965→33016→33029→33038→33047…）：
  - CDP 层**每次都成功返回**（日志 `Uploaded 'heng.png' to [INPUT] at index N`，无报错）。
  - 页面**毫无反应**——"点击上传文件或拖拽文件到这里" 提示始终在，canvas 一直空。
  - file input 索引不断变化（Semi UI 模态重渲染）。
- Agent 自行诊断（step 13 日志）："The Semi UI upload component likely requires its own event handler to be triggered, not just setting the files property." → 最终放弃封面，改用 AI 推荐封面，`done(success=False)`，Judge 判定 FAILED。

## 2. P1 改动未触碰上传路径（git 证据）

`git diff`（工作区 vs HEAD，即未提交的 P1 改动）：

| 文件 | 是否触碰上传路径 |
|---|---|
| `src/tree_walker/tools/actions.py`（`_action_upload_file` 处理器，行 1459、调用 `set_file_input` 在 1591） | **未改**（diff 为空） |
| `src/tree_walker/browser/session.py:set_file_input`（行 3674–3740） | **未改**（不在 diff；唯一相邻匹配 `_upload_temp_paths` 是我新增 `_recent_events` 缓冲时的上下文行） |
| `DOM.setFileInputFiles` 调用（行 3737） | **未改** |

`git log master..HEAD` 为空 → P1 全部是未提交的工作区修改，`master` 上跑同一示例会**同样失败**。

P1 实际改的文件：`agent.py` / `step.py`（消息分类、history、flag 接线）、`serializer.py`（**新增** `_collect_page_stats`，不改既有文件 input 检测——日志 `file_inputs=6`、`available_file_inputs=[...]` 仍正确）、`session.py`（**新增** recent_events 缓冲/CDP 监听，默认关）、`views.py` / `config.py` / `system_prompt.py`（渲染 `[Page Stats]` / `[Available Secrets]` / `[Recent Events]`，本例无 sensitive_data、recent_events 关，仅多一行 `[Page Stats]`）。均与上传机制无关。

## 3. 根因

`set_file_input`（`session.py:3674`）**只调用 `DOM.setFileInputFiles`，之后不 dispatch 任何 `change`/`input` 事件**：

```python
await self.client.send.DOM.setFileInputFiles(
    {"backendNodeId": target_id, "files": [upload_path]},
    session_id=self.current_session_id,
)
```

- 视频 input 是普通 file input → 浏览器自动触发原生 `change` → 成功。
- 封面 input 是 Semi UI 的 `semi-upload-hidden-input`（`display:none`、React 受控）→ 浏览器对 CDP 设置的 `.files` **不自动触发 React 合成 onChange** → Semi UI 上传组件收不到事件 → 页面无反应（"假成功"）。
- 代码**已有**针对此的 decoy-input 警告（`actions.py:1539` "nothing changed = you hit a hidden decoy input"，issue #96），说明这是已知的抖音/Semi UI 顽疾，非 P1 引入。

## 4. 验证 P1 无责（建议你自行执行）

我的改动未提交，可临时 stash 后在干净 master 重跑：

```powershell
git stash
uv run python examples/upload_file.py   # 封面步骤同样失败
git stash pop
```

若封面步骤在 master 上也失败（预期如此），即证明 P1 无责。

## 5. 可选的修复方向（本次不实施，仅供参考）

在 `set_file_input` 的 `DOM.setFileInputFiles` 之后，用 `Runtime.evaluate` 对该 input（经 backendNodeId 解析为 remote object）dispatch 原生 `change`+`input` 事件——这是 React/Semi UI/Ant Design Upload 类组件走 CDP 上传的标准修复（Playwright 的 `set_input_files` 即含此逻辑）。可加门控（仅对隐藏/React input 触发）避免影响普通 input。需新增测试。

> 用户已选「只要分析报告」，故本报告为最终交付，不落地任何代码。

---

# 修订（2026-07-05）：上述"P1 无责"结论被推翻——P1c 是行为回归根因

> ⚠️ **上文 §1–5 的结论（"P1 不影响上传"）只对了一半，请以本修订为准。**
> 用户反馈"页面其实显示了上传的图片"成立——封面上传**机制成功**了，但模型被
> **反馈信息误导**反复重试。P1 确实没改上传代码（§2 的 git 证据仍成立），但
> **P1c（`<agent_history>`）持久化了上传软警告**，而该警告在 P1c 之前只活一步就消失。
> 累积放大后模型误以为历次上传全失败 → 死循环重试不同 input 索引 →
> `done(success=False)`。**这是 P1c 引入的行为回归。**

## 修订 §1 现象重判（采纳用户观察）

- 上传后**页面有显示图片**（上传实际成功）；上文 §1 里"canvas 一直空 / 点击上传提示
  始终在"是**模型自己的 eval**——是被误导的结论，不是事实。
- 模型反复 `upload_file` 到不断变化的 `semi-upload-hidden-input` 索引
  （32197→32965→33016→33029→33038→33047…），每次 CDP 都成功返回，模型每次都判定
  "可能没传成功，换一个 input 重试"。

## 修订 §2 误导源：upload_file 的软警告进了 extracted_content（既有，非 P1）

`tools/actions.py:_action_upload_file`（行 1496-1542 / 1600-1602 / 1622）：
- 多 file input 共存时拼 `upload_note`（行 1536-1542）：
  `"⚠️ Page has N file inputs; uploaded to the one you specified (index X). Likely-live
  candidates (visible + upload container): [...]. If the site reacted wrongly ... retry
  upload_file on the correct visible upload area."`
- 行 1600-1602 `memory += upload_note` → 行 1622 `return ActionResult(extracted_content=memory)`。
- 即**即使上传成功**，`extracted_content` 也带"⚠️ 多 input / 候选列表 / retry..."指引。
- 该警告本身是 issue #96 的合理设计——给**紧接着的下一步**用。

## 修订 §3 P1c 把警告从"活一步"变成"活 N 步"——回归根因

`agent/agent.py:_build_agent_history_description`（P1c 新增，行 450-453）：

```python
result = h.result[0] if h.result else None
result_str = (str(result)[:200]) if result else ""   # str(result) = "EXTRACTED: Uploaded ... ⚠️ ..."
lines.append(f"  Step {h.step_number}: [{eval_}] Goal: ... | {action_str} -> {result_str}")
```

- `<agent_history>` 每步替换、滑动窗口默认 10 步（compactor 5 步）。
- step 5 的 `⚠️ ... Likely-live candidates: [...]` 在 step 6-14 **全程可见**；每个 upload
  步再加一条 → 历史里堆叠 5 条 "upload -> ⚠️ 候选列表"。
- **P1c 之前**（master/P0）：该警告只经 `[Previous Action Results]`（build_state_message
  行 140-144）影响紧接着的下一步，老步 result 不进上下文 → 一步后消失。
- **P1c 之后**：模型每步都看到"我前几步 upload 都带 ⚠️ 重试提示"→ 强信念"上传一直没
  成功"→ 死磕不同 input 索引。即使截到 200 字符，"⚠️ Page has 6 file inputs;
  Likely-live candidates: [30676, 31315, ...]"仍足以暗示"换一个试试"。

时序吻合：用户"之前"=master（含 P0、无 P1c）成功率高；P1c 后 3/3 失败。LLM 有随机性，
但机制成立 + 时序吻合 + 3/3 一致，可信度高。

## 修订 §4 证据链

- `git log master..HEAD` 空 → P1 全是未提交工作区改动，"之前"=master。
- P1 唯一动到「模型可见反馈」的子项 = **P1c**。P1a `[Page Stats]` / P1d / P1b（默认关）
  与上传反馈无关；封面模态 interactive 远 >3，不会误触 SKELETON。
- `tools/actions.py` / `session.py:set_file_input` git diff 空 → 上传机制字节级未变
  （这条仍成立——P1 没破坏上传机制，破坏的是模型的反馈回路）。

## 修订 §5 推荐修复（留待后续决定，本次不实施）

**在 `_build_agent_history_description` 把 `result_str` 换成简洁结果指示**——历史窗口只
保留每步"成功/失败/done"语义，不携带情境性软警告（警告仍由 `[Previous Action Results]`
服务紧接着的下一步，职责分明）。

`src/tree_walker/agent/agent.py` 行 450-453 改：

```python
# 旧
result = h.result[0] if h.result else None
result_str = (str(result)[:200]) if result else ""
# 新（去掉情境性软警告，只留状态）
result_str = self._summarize_step_result(h.result)
```

新增私有方法 `_summarize_step_result(results: list[ActionResult]) -> str`：

- 任一 `r.error` → `f"✗ {first_error[:80]}"`
- 任一 `r.is_done` → `"✓ done"`
- 否则 → `"✓"`

理由：`<agent_history>` 的设计意图是"让 LLM 看到早期 goal/memory，避免重复探索"——核心
是 goal + eval + memory + action，**verbose result 文本不是核心**，且 upload 的软警告
本质是情境性（下一步用）而非持久记录。`[Previous Action Results]` 不变，立即下一步仍能
看到完整警告。

- 改动局限 `agent.py`，约 12 行；向后兼容；立即生效（P1c 默认开，无新 flag）。
- **备选**（更彻底、本期不建议）：把 `_action_upload_file` 的 `upload_note` 从
  `extracted_content` 挪到 `ActionResult.metadata['note']`，让软警告只在下一步出现一次、
  不进 `__str__`/历史。需改 ActionResult 模型 + 渲染，改动面大。

## 修订 §6 验证（修复后执行）

1. **单测**（更新 `tests/test_agent_history.py`）：upload 历史（`extracted_content` 含
   "⚠️ retry"）→ 输出不含 "retry"/"⚠️"/"Likely-live"，含 `✓`；错误结果 → `✗ {error}`；
   done → `✓ done`。
2. **回归**：`uv run python -m pytest tests/ -x -v` 全过。
3. **端到端**：`uv run python examples/upload_file.py` 跑 3 次——封面上传应不再死循环。
   若仍失败，说明根因另在（如截图视觉判读），再深查。
4. **A/B 佐证**（可选）：临时把 `result_str` 改回 `str(result)[:200]` 重跑，复现死循环
   即佐证 P1c 是根因。

> 上文 §4 的「stash 后在 master 重跑会同样失败」验证思路**已作废**——master 无 P1c，
> 不会出现历史警告累积，预期封面反而能成功（或至少不死循环）。

---

# 二次修订（2026-07-05）：根因更正为 P0 state 替换 + 为什么只在抖音封面暴露

> 一次「修订」把锅扣在 P1c 上，**错了**。A/B 实测证明根因是 **P0 的 state 消息替换**。
> 本节覆盖前面所有相反结论。

## 二次 §1 A/B 实测与根因更正

| 状态 | 结果 |
|---|---|
| `f67f90b`（P0 前，state 累积） | **3/3 成功** |
| `master` `fe30c75`（P0，state 替换） | **失败**（封面步死循环换索引 58462→59245→…） |
| `master` + `enable_message_typing=false`（回退到累积） | **成功** |

唯一变量 = P0 的 `_set_state_message`（`append` → 全局唯一替换）。`enable_message_typing=false`
workaround 等价于局部回滚 P0 的替换 → 立即修复 → **坐实根因**。P1 全程未参与（在 stash）。

## 二次 §2 机制：为什么"替换"让模型判错上传成功

P0 前：每步 state **累积**，模型上下文里有 state_{N-1}（动作前）+ state_N（动作后），
对比即知"我的上传让画布多了一个 `<img>` 节点" → 判定成功。

P0 后：全局只留 1 份当前 state，模型**丧失前后对比**，只能盯当前 DOM 的噪声信号：
其他空槽位残留的"点击上传文件或拖拽文件到这里"占位文 + Semi UI hidden-input 上传后
重建导致的 input 索引变化 → 误判"上传没生效"→ 死循环换索引。

> 注：browser-use 同样用替换式 state，但有**视觉通道（screenshot）**兜底；TreeWalker
> `include_screenshot=false`（视觉未打通），全靠 DOM 文本，前后对比对它更关键。

## 二次 §3 修复 Option A（已实施）

`src/tree_walker/agent/step.py` `_set_state_message` 改为保留**最近 2 份** state
（previous + current），而非仅 1 份：

```python
state_idxs = [i for i, m in enumerate(self.messages) if m.get(_MSG_TYPE) == TYPE_STATE]
drop = set(state_idxs[:-1])  # 留最近 1 份旧 state，删更老的
self.messages = [m for i, m in enumerate(self.messages) if i not in drop]
self.messages.append({"role": "user", "content": content, _MSG_TYPE: TYPE_STATE})
```

每步 messages = […, state_{N-1}, assistant_{N-1}, state_N, …]。2 份够用（上传判定只需
紧邻的上一步对照）、有界（远小于 P0 前无界累积的 token 成本）。已更新
`tests/test_message_typing.py`（"state 恒 1 条"→"恒 2 条"），1716 测试全过，端到端验证通过。

## 二次 §4 为什么这个问题**只在抖音封面上暴露**

核心：P0 移除的是"前后对比"能力；**只有当某动作的成败在"当前 state 单独看"时就含糊不清，
才会暴露问题**。视频上传 / 普通表单的成功在当前 state 里**自证**，所以不需要前后对比；
封面上传恰好集齐了所有让信号含糊的因素。

### 视频上传（step 1）为什么没事
传完页面**直接跳转**到 `/creator-micro/content/post/video`——`[Current URL]` 变化 +
整套表单换掉 = 当前 state 自带"巨变"信号，模型一眼就知道成功，无需对比。

### 普通表单（input_text / click / select）为什么没事
- **input_text**：工具回显 "Typed 'X'"，且代码**读回 value 校验**（不一致给 `⚠️ Note`）。
- **click**：通常触发可见变化（弹窗开关、跳转、内容出现）。
- **select**：选中项直接显示。
- 元素 index 交互后**稳定不变**。
→ 成功信号在当前 state 单独看就清晰，P0 前后都正常。

### 封面上传为什么"完美踩雷"——5 个含糊因素叠加

| 因素 | 封面模态的具体表现 | 后果 |
|---|---|---|
| **不跳转** | 整个过程停在同一模态、同一 URL | 没有"巨变"信号 |
| **Semi UI 重建 hidden-input** | 上传后 file input **index 变化**（58462→59245→…） | 模型以为"我点的 input 没了 = 选错了" |
| **多槽位共存** | 横/竖/AI/收藏 4 个上传槽在同一模态 | 传完横封面后，**竖封面槽的"点击上传文件或拖拽文件到这里"占位文仍在 DOM** → 模型误判"上传区还空着" |
| **React 渲染预览，无标准 value** | 成功标志是画布渲染一个 `<img>`/canvas，不是 file input 的 value | 模型得在 ~3500 个 DOM 节点里找出"新增的那个 img"，被噪声淹没 |
| **工具回显本身含糊** | "Uploaded 'heng.png' ⚠️ ...if wrong, retry..." | 既说成功又暗示重试，无法自证 |

每个单独都不致命；叠加后：当前 state 里"画布多了个 img"被"占位文 + 变化 index + hedged
回显"三重噪声盖过。P0 前模型用 state_{N-1}（空画布）vs state_N（有 img）的对比把信号从
噪声里拣出来；P0 拿掉对照，模型只剩噪声 → 误判 → 死循环。

### 推论：不只是抖音封面

任何"效果细微 + 不跳转 + 当前 state 有干扰信号 + index 易变"的动作理论上都会中招：
其他 React 多槽位上传器（头像/图册/B 站封面等）、上传后重建 index 的 toggle/switch、
拖拽类操作。抖音封面把这几项占全了，最先暴露。Option A 对所有这类场景都有效——
恢复了前后对比，让模型能把"新增节点"从噪声里识别出来。视频上传/普通表单本来就不依赖
对比，所以无影响、也无副作用。

> **一句话**：不是 P0 专门坏了封面，而是封面是唯一一个"非用前后对比不可"的场景，P0 偏偏把那个对比拿走了。
