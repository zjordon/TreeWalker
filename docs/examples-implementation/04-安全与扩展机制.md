# Issue #4 · 安全与扩展机制（敏感数据 / 自定义动作注册）

> Epic: browser-use examples 移植 ｜ 上游方案: [`../browser-use-examples移植方案.md`](browser-use-examples移植方案.md) §5.5 / §5.6
> 实施时：创建下方"目标文件"列出的 `.py`（完整代码已给出），逐个 `uv run` 实跑。

## Issue 正文（建 issue 时用）

**标题**：`feat(examples): 安全与扩展机制（敏感数据/自定义动作）—— 移植自 browser-use`

**正文**：
移植 browser-use 中"敏感数据保护"与"自定义动作注册"两类示例：
- **敏感数据（扁平）**：TreeWalker 的 `sensitive_data` 约定与 browser-use **扁平形式一致**（`{占位符: 真实值}`，无需翻转）。机制：发往 LLM 前真实值替换为占位符，LLM 输出动作里的占位符在执行前还原为真实值（`agent.py` + `llm/client.py`）。**仅扁平可用**；browser-use 的"按域嵌套" `{'domain':{...}}` **不支持**。
- **自定义动作注册**：browser-use 用 `@controller.action(description=, domains=)`（按名注入参数）；TreeWalker 用 `@tools.registry.action(name=, description=, param_model=, terminates=)`，handler 签名固定 `(params: dict, browser: BrowserSession)`（按位置注入）。无 per-decorator `domains=`，按页过滤改用全局 `tools.apply_page_filters`。

> 内置 `upload_file` 动作已存在（受 `allowed_upload_paths` 白名单约束），因此本 issue 的"自定义动作"示例改用 `count_words` 演示注册范式，不重复造上传动作。

任务清单：
- [ ] `examples/features/sensitive_data.py` ← `features/sensitive_data.py`（仅扁平）
- [ ] `examples/custom-functions/custom_action.py` ← `custom-functions/file_upload.py`（取注册范式）

DoD：两个文件均可 `uv run` 实跑，敏感数据示例在日志里看不到真实值、自定义动作示例能被 agent 调用并返回结果。

## 目标文件

| 目标 | 来源（browser-use） |
|---|---|
| `examples/features/sensitive_data.py` | `examples/features/sensitive_data.py` |
| `examples/custom-functions/custom_action.py` | `examples/custom-functions/file_upload.py` |

## 通用前置

见 [`README.md`](README.md)「通用前置条件」。

## 实施配方

### 4.1 `examples/features/sensitive_data.py`

**来源要点**：browser-use 简单形式 `{'x_name':'my_x_name'}`（key=占位符，value=真实值）。TreeWalker 约定相同，**直接可用，无需翻转**。

**完整代码**（直接保存为 `examples/features/sensitive_data.py`）：

```python
"""Example: 敏感数据（扁平 sensitive_data）。移植自 browser-use/examples/features/sensitive_data.py（仅简单/扁平形式）。

字典约定与 browser-use 扁平形式一致：key=占位符，value=真实值，无需翻转。
机制：发往 LLM 前，真实值被替换为占位符（LLM 看不到真实值）；
LLM 输出动作里的占位符会在执行前还原成真实值。
注意：browser-use 的"按域嵌套" {'domain':{...}} TreeWalker 不支持。
前置：uv sync / chrome --remote-debugging-port=9222 / $env:ZHIPU_API_KEY="..."
运行：uv run python examples/features/sensitive_data.py
"""
import asyncio
import logging
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "src"))

from tree_walker import Agent, BrowserSession, LLMClient
from tree_walker.config import load_settings


# key=占位符（任务里用 <x_name> 引用），value=真实值
SENSITIVE_DATA = {
	"<x_name>": "my_x_name",
	"<x_password>": "my_x_password",
}

TASK = (
	"Go to https://httpbin.org/forms/post and fill the custname field with <x_name> "
	"and put <x_password> into the comment field, then submit the form."
)


async def main() -> None:
	logging.basicConfig(level=logging.INFO)
	settings = load_settings()
	if not settings.llm.api_key:
		print("Error: 请设置 ZHIPU_API_KEY 环境变量")
		sys.exit(1)
	if not settings.browser.ws_url:
		print("Error: 请先用 chrome --remote-debugging-port=9222 启动 Chrome")
		sys.exit(1)

	llm = LLMClient(settings.llm)
	browser = BrowserSession(settings.browser)
	agent = Agent(
		task=TASK,
		llm=llm,
		browser=browser,
		settings=settings.agent,
		sensitive_data=SENSITIVE_DATA,
	)
	history = await agent.run()

	if history.is_done():
		print(history.final_result())


if __name__ == "__main__":
	asyncio.run(main())
```

**适配注意点 / 验证点**：
- 验证"保护生效"：开 `logging.INFO`，观察发给 LLM 的消息里应只见 `<x_name>` / `<x_password>`，**不应**出现 `my_x_name` / `my_x_password`；而实际提交到表单的应是真实值。
- 任务文本里用 `<x_name>` 引用占位符；占位符字符串可自定义（如 `"<x_name>"` 或 `"x_name"`，二者皆可，只要任务和 dict key 一致）。

### 4.2 `examples/custom-functions/custom_action.py`

**完整代码**（直接保存为 `examples/custom-functions/custom_action.py`）：

```python
"""Example: 自定义动作注册范式。移植自 browser-use/examples/custom-functions/file_upload.py（取其注册范式；内置 upload_file 已存在，故用 count_words 演示）。

TreeWalker 注册自定义动作的标准范式：
- Tools() 先注册默认 22 个动作；
- @tools.registry.action(name=, description=, param_model=, terminates=) 注册新动作；
- handler 签名固定 (params: dict, browser: BrowserSession) -> ActionResult|str|None
  （按位置注入，非 browser-use 的按名注入）；
- 无 per-decorator domains=；按页过滤改用 tools.apply_page_filters({动作名:[glob]})。
前置：uv sync / chrome --remote-debugging-port=9222 / $env:ZHIPU_API_KEY="..."
运行：uv run python examples/custom-functions/custom_action.py
"""
import asyncio
import logging
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "src"))

from pydantic import BaseModel, Field

from tree_walker import Agent, ActionResult, BrowserSession, LLMClient, Tools
from tree_walker.config import load_settings


class CountParams(BaseModel):
	text: str = Field(..., description="要统计单词数的文本")


def build_tools() -> Tools:
	tools = Tools()   # 先注册默认动作（参数全可省，见 tools/actions.py:357）

	@tools.registry.action(
		name="count_words",
		description="Count the number of words in the given text and return the result.",
		param_model=CountParams,
		terminates=False,
	)
	async def _count_words(params: dict, browser: BrowserSession):
		# 注意签名：TreeWalker 按位置注入 (params, browser)，不是按名注入
		n = len(params["text"].split())
		return ActionResult(extracted_content=f"word count = {n}")

	return tools


TASK = (
	"Use the count_words action to count the words in "
	"'hello world from tree walker' and tell me the result."
)


async def main() -> None:
	logging.basicConfig(level=logging.INFO)
	settings = load_settings()
	if not settings.llm.api_key:
		print("Error: 请设置 ZHIPU_API_KEY 环境变量")
		sys.exit(1)
	if not settings.browser.ws_url:
		print("Error: 请先用 chrome --remote-debugging-port=9222 启动 Chrome")
		sys.exit(1)

	llm = LLMClient(settings.llm)
	browser = BrowserSession(settings.browser)
	agent = Agent(
		task=TASK,
		llm=llm,
		browser=browser,
		settings=settings.agent,
		tools=build_tools(),   # 传入自定义 Tools
	)
	history = await agent.run()

	if history.is_done():
		print(history.final_result())


if __name__ == "__main__":
	asyncio.run(main())
```

**适配注意点 / 验证点**：
- 任务里明确要求"use the count_words action"，确保 agent 真的去调用自定义动作；输出应包含 `word count = 5`。
- 若要演示"按页过滤"（browser-use 的 `domains=`），可在 `build_tools()` 里加：`tools.apply_page_filters({"count_words": ["*.example.com"]})`，使该动作仅在匹配页可用。
- handler 返回 `ActionResult(extracted_content=...)` 即可被 agent 读到；`ActionResult`、`Tools` 从 `tree_walker` 顶层导入。

## 验收标准（DoD）

- 两个 `.py` 文件分别存在于 `examples/features/`（sensitive_data）与 `examples/custom-functions/`（custom_action）下。
- `sensitive_data.py`：日志中发给 LLM 的内容不含真实值；表单提交了真实值。
- `custom_action.py`：agent 实际调用了 `count_words` 并返回 `word count = 5`。

## 验证方式

```
uv run python examples/features/sensitive_data.py
uv run python examples/custom-functions/custom_action.py
```

## 风险 / caveat

- 敏感数据的"按域嵌套"形式（browser-use 高级用法）TreeWalker 不支持；如需按域隔离凭据，需先在 TreeWalker 补能力（独立功能开发，不在本批）。
- 自定义动作的按页过滤是全局配置（`apply_page_filters` / `action_page_filters`），不如 browser-use 的 per-decorator `domains=` 灵活；本 issue 不涉及 `src/` 改动。
