# Issue #7 · 入门补全（表单填写 / 多步任务）

> Epic: browser-use examples 移植 ｜ 上游方案: [`../browser-use-examples移植方案.md`](browser-use-examples移植方案.md) §4 getting_started
> 实施时：创建下方"目标文件"列出的 `.py`（完整代码已给出），逐个 `uv run` 实跑。

## Issue 正文（建 issue 时用）

**标题**：`feat(examples): 入门补全（表单填写/多步任务）—— 移植自 browser-use`

**正文**：
补全 browser-use `getting_started` 系列中尚未在 TreeWalker 覆盖的两个入门示例（`01_basic_search` 已有 `examples/basic_agent.py`、`03_data_extraction` 在 Issue #1、`05_fast_agent` 在 Issue #3，本 issue 补 02 和 04）。两个都是**纯任务驱动**，几乎只改连接方式：
- **表单填写**：在 httpbin 表单填多项并提交（agent 用 `input_text` 链式填多个字段 + `click` 提交）。
- **多步任务**：搜索 → 进结果 → 抽取 → 汇报（多步推理）。

任务清单：
- [ ] `examples/getting_started/form_filling.py` ← `getting_started/02_form_filling.py`
- [ ] `examples/getting_started/multi_step_task.py` ← `getting_started/04_multi_step_task.py`

DoD：两个文件均可 `uv run` 实跑，分别完成表单提交 / 搜索-抽取多步流程。

## 目标文件

| 目标 | 来源（browser-use） |
|---|---|
| `examples/getting_started/form_filling.py` | `examples/getting_started/02_form_filling.py` |
| `examples/getting_started/multi_step_task.py` | `examples/getting_started/04_multi_step_task.py` |

## 通用前置

见 [`README.md`](README.md)「通用前置条件」。

## 实施配方

### 7.1 `examples/getting_started/form_filling.py`

**完整代码**（直接保存为 `examples/getting_started/form_filling.py`）：

```python
"""Example: 表单填写（多字段 + 提交）。移植自 browser-use/examples/getting_started/02_form_filling.py。

在 httpbin 表单页填多项并提交。演示任务驱动的表单填写：
agent 会用 input_text 链式填多个字段，再 click 提交。
前置：uv sync / chrome --remote-debugging-port=9222 / $env:ZHIPU_API_KEY="..."
运行：uv run python examples/getting_started/form_filling.py
"""
import asyncio
import logging
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "src"))

from tree_walker import Agent, BrowserSession, LLMClient
from tree_walker.config import load_settings


TASK = (
	"Go to https://httpbin.org/forms/post . Fill the form with: "
	"custname='John Doe', custtel='555-1234', custemail='john@example.com', "
	"and select size='large'. Then submit the form and tell me what the response page shows."
)


async def main() -> None:
	logging.basicConfig(level=logging.INFO)
	settings = load_settings()
	if not settings.llm.api_key:
		print("Error: 请设置 ZHIPU_API_KEY 环境变量")
		sys.exit(1)
	if not settings.browser.ws_url:
		print("Error: 请先用 chrome --remote-debugging-port=9222 启动 Chrome")
		sys.exit(1)

	llm = LLMClient(settings.llm)
	browser = BrowserSession(settings.browser)
	agent = Agent(task=TASK, llm=llm, browser=browser, settings=settings.agent)
	history = await agent.run()

	if history.is_done():
		print(history.final_result())


if __name__ == "__main__":
	asyncio.run(main())
```

**适配注意点**：httpbin 表单的 `size` 是 radio；agent 应能处理。提交后 httpbin 会回显 JSON，`final_result()` 里应能看到填入的字段值。

### 7.2 `examples/getting_started/multi_step_task.py`

**完整代码**（直接保存为 `examples/getting_started/multi_step_task.py`）：

```python
"""Example: 多步任务（搜索 → 进结果 → 抽取 → 汇报）。移植自 browser-use/examples/getting_started/04_multi_step_task.py。

演示多步推理：搜索某主题，打开第一个结果，抽取一段定义，汇报来源 URL 与定义。
前置：uv sync / chrome --remote-debugging-port=9222 / $env:ZHIPU_API_KEY="..."
运行：uv run python examples/getting_started/multi_step_task.py
"""
import asyncio
import logging
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[2] / "src"))

from tree_walker import Agent, BrowserSession, LLMClient
from tree_walker.config import load_settings


TASK = (
	"Go to https://www.google.com and search for 'what is browser automation'. "
	"Open the first result, extract a one-paragraph definition of browser automation "
	"from that page, then tell me the source URL and the definition."
)


async def main() -> None:
	logging.basicConfig(level=logging.INFO)
	settings = load_settings()
	if not settings.llm.api_key:
		print("Error: 请设置 ZHIPU_API_KEY 环境变量")
		sys.exit(1)
	if not settings.browser.ws_url:
		print("Error: 请先用 chrome --remote-debugging-port=9222 启动 Chrome")
		sys.exit(1)

	llm = LLMClient(settings.llm)
	browser = BrowserSession(settings.browser)
	agent = Agent(task=TASK, llm=llm, browser=browser, settings=settings.agent)
	history = await agent.run()

	if history.is_done():
		print(history.final_result())


if __name__ == "__main__":
	asyncio.run(main())
```

**适配注意点**：Google 搜索结果页结构偶有变动 / 地区差异；若 agent 打不开第一个结果，可把任务里的搜索引擎换成更稳定的（如 DuckDuckGo `https://duckduckgo.com`）。

## 验收标准（DoD）

- 两个 `.py` 文件均存在于 `examples/getting_started/` 下。
- `form_filling.py`：提交表单后 `final_result()` 含回显的字段值。
- `multi_step_task.py`：完成搜索→进结果→抽取，输出含来源 URL 与一段定义。

## 验证方式

```
uv run python examples/getting_started/form_filling.py
uv run python examples/getting_started/multi_step_task.py
```

## 风险 / caveat

- 入门示例依赖公网站点可访问；Google/DuckDuckGo 结果页结构变动会影响成功率，验证失败时优先怀疑站点。
- 本 issue 不涉及 `src/` 改动。
