# 5: 终结化 (_finalize)

> 源码位置: `src/tree_walker/agent/step.py` 第 614-666 行

## 📋 Phase 概述

`_finalize()` 在 `_step()` 的 `finally` 块中调用，**无论成功还是异常都保证执行**。负责：记录执行历史、发射步结束事件、递增步数计数器。

## 🔄 主要逻辑流程

```
_finalize(browser_state, model_output, results) → None
  │
  ├── 1. 历史记录与事件发射                       (L625-648) → [详细文档](1-历史记录与事件发射.md)
  │     ├── if model_output is not None:
  │     │     ├── 构建 state_summary {url, title, duration}
  │     │     └── history.history.append(AgentHistory(...))
  │     └── if _obs_bus:
  │           └── emit StepEndEvent {step, duration, is_done, consecutive_failures}
  │
  └── 2. 日志总结与步数递增                       (L650-651) → [详细文档](2-日志总结与步数递增.md)
        ├── _log_step_completion_summary(results)
        └── state.n_steps += 1  (最后执行)
```

## 💡 总结

| 子步骤 | 源码行号 | 主要操作 | 输出 |
|--------|---------|---------|------|
| 历史记录与事件发射 | L625-648 | history.append + StepEndEvent | 更新 history |
| 日志总结与步数递增 | L650-651 | duration/ok/err 日志 + n_steps++ | 更新 state |
