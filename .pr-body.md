## 概述

兑现 #78 —— `read_file` 阶段二的 4 个未实现项（`encoding`/`newline` 已随 write_file/replace_file 阶段二落地）。完整方案见 [`docs/tools-optimize/read_file_follow_up.md`](docs/tools-optimize/read_file_follow_up.md)。

## 改动

- **二.A `offset`/`limit` 字符级分页** —— `ReadFileParams` 加 `offset`（默认 0）/`limit`（默认 `None`→`read_file_max_chars`）；新增 `_window_and_echo` 共享 helper；截断 footer 追加 `use offset=N to continue`，让 LLM 翻读超大文件。默认 offset=0 完全保持阶段一行为（footer 子串断言兼容，阶段一测试不破）。
- **二.B 二进制嗅探** —— `_sniff_file_kind()`（magic bytes 主 + 扩展名辅）：`.exe`/`.zip` 等给可操作 error（早拒，优于 `UnicodeDecodeError` 啰嗦堆栈），PDF/DOCX/图片转 二.D，普通文本走原解码路径。
- **二.C `allowed_read_paths` 白名单** —— 7 处对称镜像 `allowed_write_paths`（config 字段 + `_load_allowed_read_paths` + env `AGENT_ALLOWED_READ_PATHS` + wiring + `Tools.__init__` + `agent.py` 接线 + action 守卫）；默认 `None`=全放行（保持现状「read anywhere」）。
- **二.D 富文档** —— 新增 optional extra `[docs]`（`pypdf` + `python-docx`）；PDF/DOCX 抽文本走 `_window_and_echo`，缺依赖给安装提示。

## 关键决策

**`metadata["images"]` 是死代码** —— `evaluate` 写了它，但 agent loop（`step.py`）只取 `extracted_content` 喂 LLM，从不读 `metadata`。故**图片不走 base64 通道**（会撑上下文又无收益），只给可操作提示；PDF/DOCX 产文本不受影响。待 agent loop 统一接线 images 通道（届时也激活 evaluate）后再启用图片。

## 测试 / 覆盖率

- `tests/test_read_file.py`：50 passed（PDF/DOCX 用 `importorskip` + monkeypatch）
- 全量回归：1489 passed，0 失败
- 覆盖率：`actions.py` 95%、总计 90%（>85%）

Closes #78
